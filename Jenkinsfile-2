pipeline {
    agent any
    stages {
        stage("Build") {
            step {
                echo 'Build done!'
            }
        }
        stage("Test") {
            step {
                echo 'Test done!'
            }
        }
        stage("Deploy") {
            step {
                echo 'Deploy done!'
            }
        }
    }
}
